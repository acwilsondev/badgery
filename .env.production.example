# Node environment setting (development/staging/production)
NODE_ENV=production

# Port for the application to listen on
PORT=8080

# Logging level (debug/info/warn/error)
# Using warn level for production to minimize logging overhead
LOG_LEVEL=warn

